{
  "name": "skia-site",
  "private": true,
  "version": "0.0.0",
  "description": "Static marketing site for Skia. This is not the CLI package and is not published to npm.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.2.6",
    "@fontsource/ibm-plex-sans": "^5.2.6",
    "@fontsource/newsreader": "^5.2.8"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
