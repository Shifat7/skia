---
title: Skia
description: Alpha local pre-PR checkpoint. The foundation ships in the repository. The product CLI is not released.
template: splash
sidebar:
  hidden: true
prev: false
next: false
banner:
  content: |
    <a href="/skia/status/">Alpha · foundation shipping · product CLI not released → /status/</a>
hero:
  title: Comprehend AI-written diffs before you open the PR.
  tagline: Local-first reading check — source-backed simplify, one prediction you make yourself, uncertainty left visible. Not a GitHub App. Not a review bot. Not a runnable <code>skia review</code> yet.
  actions:
    - text: View on GitHub
      link: https://github.com/Shifat7/skia
      icon: external
      variant: primary
    - text: What's real vs planned
      link: /skia/status/
      icon: right-arrow
      variant: secondary
    - text: Limitations
      link: '#limitations'
      variant: minimal
---

import { LinkButton, Steps } from '@astrojs/starlight/components';

<ul class="trust-strip" aria-label="Trust substitutes">
	<li>Local-first</li>
	<li>Source-backed</li>
	<li>Uncertainty visible</li>
	<li>Telemetry none planned</li>
</ul>

## Problem

AI tools can ship a working-looking diff faster than a reliable mental model forms.

- Extra abstractions or dependencies the change did not need
- Branches and error paths buried in a large generated diff
- Code that compiles and passes tests while the behavior is still unfamiliar

Skia is the intended checkpoint for one developer before that gap becomes a reviewer's problem.

## How it works

`skia review` is a documented name. The shell in `src/main.ts` is unimplemented. Intended loop:

<Steps>

1. Stage or point at a change — exact Git snapshot when the product CLI lands.

2. Read the simplified view. It is a map, and it is not executable.

3. Predict one result yourself, before any feedback.

4. Open original code wherever coverage is `partial`, `unmapped`, `unsupported`, `failed`, or `unchecked`.

</Steps>

## Available vs planned

Solid badges are in the repository today. Outline badges are intended product behavior — never a green check.

<table class="capability-matrix">
	<thead>
		<tr>
			<th>Available now</th>
			<th>Planned</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td>
				<span class="status-badge status-badge--ships">Ships today</span>
				Exact Git snapshots, schema and coverage validation, TypeScript / TSX / Python analysis, path-safe local storage.
			</td>
			<td>
				<span class="status-badge status-badge--planned">Planned</span>
				Simplified view + one prediction, <code>skia review</code> / <code>skia repo review</code>, npm publish, editor and CI adapters.
			</td>
		</tr>
	</tbody>
</table>

<p class="matrix-note">Full matrix, including exclusions: <a href="/skia/status/">Status</a>.</p>

## Privacy and egress

Skia has no review backend and no product telemetry. Foundation artifacts stay on disk. A future repository mode may send bounded context only to a model endpoint you configure, after consent. “Local” is about where artifacts sit — not a promise that a cloud model never sees a prompt.

<table class="egress-matrix">
	<thead>
		<tr>
			<th>Path</th>
			<th>Status</th>
			<th>What can leave</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td>Foundation library</td>
			<td><span class="status-badge status-badge--ships">Ships today</span></td>
			<td>Designed as local snapshot, parse, and storage. Not a formal network audit.</td>
		</tr>
		<tr>
			<td>Staged review</td>
			<td><span class="status-badge status-badge--planned">Planned</span></td>
			<td>Specified with no network capability.</td>
		</tr>
		<tr>
			<td>Repo-mode model</td>
			<td><span class="status-badge status-badge--planned">Planned</span></td>
			<td>Only the provider endpoint you disclose, after consent.</td>
		</tr>
		<tr>
			<td>Product telemetry</td>
			<td>None</td>
			<td>No analytics beacon, crash upload, or auto-update phone-home.</td>
		</tr>
	</tbody>
</table>

<p class="matrix-note">Details: <a href="/skia/privacy/">Privacy</a>.</p>

## Limitations

- No working review CLI. Package is private at `0.0.0`; shell returns unimplemented.
- No global install command is shown here. npm is the first channel when publication is allowed.
- Simplified views will not be executable or proof of semantic equivalence.
- Name collision with Google’s Skia graphics library is an open release risk ([OD-1](https://github.com/Shifat7/skia/blob/main/docs/OPEN_DECISIONS.md)).

<p class="matrix-note">More on <a href="/skia/status/">Status</a> · hand-written transcripts in <a href="https://github.com/Shifat7/skia/tree/main/examples/illustrative">examples/illustrative</a>.</p>

## FAQ

<div class="faq">
	<details>
		<summary>Is <code>skia review</code> available today?</summary>
		<p>No. The package is private at 0.0.0, and the shell does not run a review. There is no working global npm install, Homebrew formula, or curl installer.</p>
	</details>
	<details>
		<summary>Does my code leave the machine?</summary>
		<p>Foundation work is designed to keep snapshot and parse artifacts local. Skia does not operate a review backend and does not send product telemetry. A future repository mode may send bounded context to an endpoint you configure, after consent. See <a href="/skia/privacy/">Privacy</a>.</p>
	</details>
	<details>
		<summary>Is this a CodeRabbit or Greptile replacement?</summary>
		<p>No. Skia is intended as a local pre-PR comprehension checkpoint for one developer — simplify, predict, surface uncertainty — not a hosted PR bot or agent fix loop.</p>
	</details>
	<details>
		<summary>How do I install it?</summary>
		<p>You do not install a review CLI yet. Contributors run the foundation with Node 24+ from the repository (<a href="/skia/contribute/">Contribute</a>). No <code>npm i -g skia</code> command is published here.</p>
	</details>
	<details>
		<summary>Which languages are analyzed today?</summary>
		<p>Exactly TypeScript, TSX, and Python. Anything else stays <code>unsupported</code>.</p>
	</details>
	<details>
		<summary>Is this Google's Skia?</summary>
		<p>No. Different project. The shared name is an open release risk (<a href="https://github.com/Shifat7/skia/blob/main/docs/OPEN_DECISIONS.md">OD-1</a>), not an affiliation.</p>
	</details>
</div>

## Start from the repository

Build the pre-PR habit from the source of truth. Marketing drafts stay unpublished until a stranger can run a real review.

<div class="cta-row">
	<LinkButton href="https://github.com/Shifat7/skia" variant="primary" icon="external">View on GitHub</LinkButton>
	<LinkButton href="/skia/status/" variant="secondary" icon="right-arrow">What's real vs planned</LinkButton>
	<LinkButton href="#limitations" variant="minimal">Limitations</LinkButton>
</div>
